1. git init
   --初始化本地GIT仓储
   就是在本地文件夹中添加了一个.git的文件夹用于记录所有的项目变更信息

2. git status
   --用于查看本地仓储的状态
   --第一次查看，显示的是所有没有被跟踪的文件

3. git add
   --添加本地暂存(托管)文件
   类似于node_modules这种性质的文件没有必要被跟踪

4. git ignore
   --添加本地git忽略清单文件
   在代码库文件夹的根目录添加一个.gitignore文件
   此文件用于说明忽略的文件有哪些